# Pedirle un número al usuario
valor = input("Escribe un número: ")

# Convirtiendo el valor a entero y lo multiplicamos por 2
resultado = int(valor) * 2
print(resultado)

# Convirtiendo el valor a flotante y lo multiplicamos por 2
resultado = float(valor) * 2
print(resultado)